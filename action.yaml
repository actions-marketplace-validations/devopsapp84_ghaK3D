name: 'Create K3d Temporary Cluster'
description: "⎈ Github action to helm releases testings"
author: 'Janusz Kujawa <janusz.kujawa@gmail.com>'
inputs:
  name:
    description: "Cluster name: "
    required: true
  size:
    description: "Size based on config file: "
    required: false
  k3d-version:
    description: "K3d version: "
    required: false
runs:
  using: composite
  steps:
    - id: main
      shell: bash
      run: ${{ github.action_path }}/bin/run2.sh deploy
      env:
        CLUSTER_NAME: ${{ inputs.cluster-name }}
        ARGS: ${{ inputs.args }}
        K3D_VERSION: ${{ inputs.k3d-version }}
